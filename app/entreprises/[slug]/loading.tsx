/**
 * Keep the route-level fallback for Next.js streaming, but do not paint a
 * blocking loading card: the public page should appear as a normal web page.
 */
export default function InrSearchLoading() {
  return null;
}
